# Local secrets — never commit real keys
.env
.env.*
!.env.example

Trial 1.xlsx
# Python
__pycache__/
*.py[cod]
.Python
.venv/
venv/
*.egg-info/
dist/
build/
prompt.txt
# OS / editor
.DS_Store
.idea/
.vscode/

# Streamlit local secrets (Cloud uses dashboard instead)
.streamlit/secrets.toml

# Optional: exclude huge local index from git (then deploy index via LFS or CI)
# chroma_db/
