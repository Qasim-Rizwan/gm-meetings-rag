# Local secrets — never commit real keys
.env
.env.*
!.env.example

Trial 1.xlsx
# Python
__pycache__/
*.py[cod]
.Python
.venv/
venv/
*.egg-info/
dist/
build/
# OS / editor
.DS_Store
.idea/
.vscode/
# Streamlit local secrets (Cloud uses dashboard instead)
.streamlit/secrets.toml

# ChromaDB vector index — excluded from git for security.
# The index lives only on the server/locally and is never committed to the repo.
chroma_db/
